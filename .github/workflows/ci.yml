name: NCC CI/CD Quality Pipeline

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  validate-and-test:
    name: Lint, Typecheck, Test & Build Validation
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [22.x]

    steps:
      - name: Checkout Source Code
        uses: actions/checkout@v4

      - name: Setup Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: "npm"

      - name: Install Dependencies
        run: npm ci

      - name: Validate Prisma Schema
        run: npx prisma validate

      - name: Run ESLint Static Analysis
        run: npm run lint

      - name: Run Typecheck Analysis
        run: npm run typecheck

      - name: Run Automated Test Suite
        run: npm run test
        env:
          NODE_ENV: test
          JWT_SECRET: ci-test-jwt-secret-key-32-chars-long
          SESSION_SECRET: ci-test-session-secret-key-32-bytes
          VITE_SUPABASE_URL: https://qsrmzajadmmgqhfbxdwu.supabase.co
          VITE_SUPABASE_ANON_KEY: sb_publishable_ru09YlOy1LNorCk6y-SD2w_daWCqr6U
          SUPABASE_URL: https://qsrmzajadmmgqhfbxdwu.supabase.co
          SUPABASE_ANON_KEY: sb_publishable_ru09YlOy1LNorCk6y-SD2w_daWCqr6U

      - name: Compile Production Bundle
        run: npm run build
        env:
          NODE_ENV: production
          VITE_SUPABASE_URL: https://qsrmzajadmmgqhfbxdwu.supabase.co
          VITE_SUPABASE_ANON_KEY: sb_publishable_ru09YlOy1LNorCk6y-SD2w_daWCqr6U
          SUPABASE_URL: https://qsrmzajadmmgqhfbxdwu.supabase.co
          SUPABASE_ANON_KEY: sb_publishable_ru09YlOy1LNorCk6y-SD2w_daWCqr6U

      - name: Security Vulnerability Audit
        run: npm audit --audit-level=high
        continue-on-error: true

      - name: Validate Docker Image Build
        run: docker build -t ncc-crm:ci .
